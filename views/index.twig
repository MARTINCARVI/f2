{% extends "base.twig" %}  {# Uses base.twig as template #}

{% block content %}
    {# Generic content for when no specific template exists #}
    {% for post in posts %}
        <article>
            <h2>{{ post.title }}</h2>
            <div>{{ post.content }}</div>
        </article>
    {% endfor %}
{% endblock %}